[build]
  command   = "echo no-build"
  publish   = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# SPA-маршруты (если используешь #/ — можно и без этого блока)
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
